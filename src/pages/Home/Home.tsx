import { Center, Title } from "@mantine/core"

const Home = () => {
  return <Center><Title order={3}>🧐 Find github entities</Title></Center>
}

// eslint-disable-next-line import/no-default-export
export default Home
